function getRequest(){
        cat $line | grep -nEa 'HTTP\/1\.1.+' | cut -d ':' -f 1 | xargs -I @ sh -c "head -n @ waste | awk '{ if (NR==@) { sub(/HTTP.*/, \"\"); print } else {print } }' > $1_dup"; mv $1_dup $1
}
